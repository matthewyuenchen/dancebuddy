# API dependencies. Installed alongside the backend deps (the API runs the pipeline in-process).
fastapi>=0.110
uvicorn[standard]>=0.29
hypercorn>=0.16
python-multipart>=0.0.9
httpx>=0.27
pytest>=8.0
